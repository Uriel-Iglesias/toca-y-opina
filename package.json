{
  "name": "toca-y-opina",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page de Toca y Opina (stands NFC + QR para reseñas en Google).",
  "scripts": {
    "build": "tailwindcss -i src/input.css -o assets/tailwind.css --minify",
    "dev": "tailwindcss -i src/input.css -o assets/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  },
  "dependencies": {
    "framer-motion": "^12.38.0"
  }
}
